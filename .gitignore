node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

# Env — NEVER commit these
.env
.env.*
!.env.example
!.env.test

# SQLite database files — data lives in Docker volumes, not the repo
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
data/

# IDE
.vscode/settings.json
.idea/

# Claude worktrees (embedded git repos)
.claude/worktrees/

# Logs
*.log
npm-debug.log*

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local screenshots / design references — not part of the app
screens/
